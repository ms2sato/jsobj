/**
 * この中に以下の仕様でクラスを作りましょう。
 *
 * CLOCk.Clockとして公開される
 * start、stopのメソッドを持つ。
 * 一秒ごとに、自身の持つ現在時刻（now）が更新されるようにして下さい。
 *
 * onStart、onStop、onTickのメソッドを定義しましょう（呼ばれる事が確認できれば中身は空で良いです）。
 * それぞれ、スタートした時、ストップしたとき、現在時刻が更新されたときに呼ばれます。
 *
 * check.htmlに、それぞれテストが書かれています。これで動かせるようにして下さい。
 */